import SellerVerificationConsentClient from '@/components/seller/SellerVerificationConsentClient';

export const metadata = {
  title: 'Verification consent',
  description: 'Consent before merchant identity verification',
};

export default function AppSellerVerificationConsentPage() {
  return <SellerVerificationConsentClient />;
}
