import SellerVerificationClient from '@/components/seller/SellerVerificationClient';

export const metadata = {
  title: 'Verification',
  description: 'Merchant identity verification on StoreLink',
};

export default function AppSellerVerificationPage() {
  return <SellerVerificationClient />;
}
